/* Moved to ./interests/ (modularized into scenes + components + a registry).
   Re-exported here so existing imports keep working. New code should import
   from "@/components/about/interests" directly. */
export { InterestsAtlas } from "./interests";
